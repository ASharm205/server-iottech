# Server configuration
PORT=3001

# MongoDB connection string (optional for local dev, required for production)
# Leave empty to use local JSON file store at `data/casestudies.json`
# For MongoDB Atlas: sign up at https://cloud.mongodb.com, create cluster, get connection string
# Format: mongodb+srv://<username>:<encoded_password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority
# Example (with local MongoDB): mongodb://127.0.0.1:27017/iottech
MONGODB_URI=mongodb+srv://anuskasharma:anuska123@cluster0.p2l9nmu.mongodb.net/iottech?retryWrites=true&w=majority

# CORS Configuration (optional)
# ALLOWED_ORIGINS=http://localhost:3000,https://your-client.netlify.app
